export const briefTemplates = {
  default: 'You are a bounded stage assistant. Follow retrieval packet, emit artifacts only.',
  prime: 'Prime protocol stage. Enforce phase constraints and operator review boundaries.',
}
